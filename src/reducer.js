import {CombineReducers} from 'redux';

const defaultState={

}



function projectCardReducer(state=defaultState.BLAH, action){
    switch(action.type){
        // toggle have / need tool.
        case ""
    }
}


const reducer=combineReducers({

})